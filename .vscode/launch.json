{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug upload.ps1 DryRun",
            "type": "PowerShell",
            "request": "launch",
            "script": "${workspaceFolder}/upload.ps1",
            "args": ["-DryRun"],
            "cwd": "${workspaceFolder}"
        }
    ]
}
